app {
  routes {
    # If ask takes more time than this to complete the request is failed
    ask-timeout = 5s
  }
}

github {
    username = "username"
    username = ${?GH_USERNAME}
    token = ""
    token = ${?GH_TOKEN}
}